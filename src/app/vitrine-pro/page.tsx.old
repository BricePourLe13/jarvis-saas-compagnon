"use client";

import React, { useEffect } from "react";
import { motion } from "motion/react";

import { TypewriterEffectSmooth } from "@/components/ui/typewriter-effect";
import { FloatingDock } from "@/components/ui/floating-dock";
import { BentoGrid, BentoGridItem } from "@/components/ui/bento-grid";
import { Timeline } from "@/components/ui/timeline";
import { MacbookScroll } from "@/components/ui/macbook-scroll";
import { ContainerScrollAnimation } from "@/components/ui/container-scroll-animation";
import Avatar3D from "@/components/kiosk/Avatar3D";
import { 
  VscHome, 
  VscGraph,
  VscRobot,
  VscMail,
  VscPlay,
  VscShield,
  VscStar,
  VscGear,
  VscArrowUp,
  VscPieChart
} from 'react-icons/vsc';

export default function VitrineProfessionnellePage() {
  // Force dark mode
  useEffect(() => {
    document.documentElement.classList.add('dark');
  }, []);

  const heroWords = [
    { text: "Révolutionnez", className: "text-white" },
    { text: "votre", className: "text-white" },
    { text: "salle", className: "text-blue-400" },
    { text: "avec", className: "text-white" },
    { text: "l'IA", className: "text-green-400" },
  ];

  const dockItems = [
    {
      title: "Accueil",
      icon: <VscHome className="h-full w-full text-neutral-300" />,
      href: "#hero",
    },
    {
      title: "Solution", 
      icon: <VscRobot className="h-full w-full text-neutral-300" />,
      href: "#solution",
    },
    {
      title: "Démo",
      icon: <VscPlay className="h-full w-full text-neutral-300" />,
      href: "#demo",
    },
    {
      title: "ROI",
      icon: <VscArrowUp className="h-full w-full text-neutral-300" />,
      href: "#roi",
    },
    {
      title: "Fonctionnalités",
      icon: <VscGear className="h-full w-full text-neutral-300" />,
      href: "#features",
    },
    {
      title: "Contact",
      icon: <VscMail className="h-full w-full text-neutral-300" />,
      href: "#contact",
    },
  ];

  const statsData = [
    { number: "67%", label: "Réduction du churn", icon: "📉" },
    { number: "24/7", label: "Disponibilité IA", icon: "🤖" },
    { number: "5x", label: "ROI moyen", icon: "💰" },
    { number: "98%", label: "Satisfaction clients", icon: "⭐" },
  ];

  const problemPoints = [
    {
      title: "Churn Élevé",
      description: "67% des membres abandonnent avant 6 mois",
      icon: "😰",
      impact: "Perte de revenus massive"
    },
    {
      title: "Support Limité",
      description: "Équipe débordée, membres frustrés",
      icon: "😵",
      impact: "Expérience dégradée"
    },
    {
      title: "Pas de Données",
      description: "Aucune visibilité sur la satisfaction",
      icon: "🔍",
      impact: "Décisions à l'aveugle"
    },
    {
      title: "Coûts Cachés",
      description: "Recrutement constant de nouveaux membres",
      icon: "💸",
      impact: "Budget marketing explosé"
    },
  ];

  const solutionFeatures = [
    {
      title: "IA Conversationnelle",
      description: "Miroir digital interactif avec IA speech-to-speech",
      benefits: ["Réponses instantanées", "Personnalisation totale", "Support 24/7"],
      icon: <VscRobot className="h-8 w-8 text-blue-400" />
    },
    {
      title: "Dashboard Analytics",
      description: "Insights IA pour optimiser votre salle",
      benefits: ["Prédiction churn", "Recommandations IA", "KPIs temps réel"],
      icon: <VscGraph className="h-8 w-8 text-green-400" />
    },
    {
      title: "Génération de Revenus",
      description: "Partenariats marques intégrés",
      benefits: ["Revenus passifs", "Amortissement rapide", "ROI garanti"],
      icon: <VscPieChart className="h-8 w-8 text-yellow-400" />
    }
  ];

  const roiCalculation = [
    {
      metric: "Réduction du churn",
      before: "67% abandonnent",
      after: "22% abandonnent", 
      impact: "+45% rétention",
      value: "+35k€/an"
    },
    {
      metric: "Coûts acquisition",
      before: "150€ par nouveau membre",
      after: "50€ par nouveau membre",
      impact: "-67% coûts",
      value: "+20k€/an"
    },
    {
      metric: "Revenus partenariats",
      before: "0€",
      after: "10k€/mois",
      impact: "Nouveau canal",
      value: "+120k€/an"
    }
  ];

  return (
    <div className="dark bg-black min-h-screen">
      {/* FLOATING DOCK */}
      <div className="fixed bottom-10 left-1/2 transform -translate-x-1/2 z-50">
        <FloatingDock
          items={dockItems}
          desktopClassName="bg-black/80 border border-neutral-700 backdrop-blur-md"
          mobileClassName="bg-black/80 border border-neutral-700 backdrop-blur-md"
        />
      </div>

      {/* 🌟 HERO SECTION */}
      <section id="hero" className="relative min-h-screen overflow-hidden">
        <BackgroundGradientAnimation
          gradientBackgroundBlendMode="overlay"
          className="absolute inset-0"
          containerClassName="absolute inset-0"
          colors={[
            "#000000",
            "#1a1a1a",
            "#2d2d2d",
            "#404040",
            "#333333",
            "#262626",
          ]}
          opacity={0.8}
          speed={3}
        >
          <div className="absolute inset-0 z-10 flex items-center">
            <div className="container mx-auto px-4">
              <div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
                
                {/* GAUCHE - CONTENU */}
                <div className="space-y-8 text-center lg:text-left">
                  
                  {/* LOGO/MARQUE */}
                  <motion.div
                    initial={{ opacity: 0, y: -20 }}
                    animate={{ opacity: 1, y: 0 }}
                    transition={{ duration: 0.8 }}
                    className="flex items-center justify-center lg:justify-start space-x-3"
                  >
                    <div className="w-12 h-12 bg-gray-800 rounded-lg flex items-center justify-center">
                      <VscRobot className="h-6 w-6 text-gray-300" />
                    </div>
                    <span className="text-2xl font-bold text-gray-100">JARVIS GROUP</span>
                  </motion.div>
                  
                  {/* TYPEWRITER TITLE */}
                  <div className="space-y-4">
                    <TypewriterEffectSmooth 
                      words={heroWords}
                      className="text-left text-4xl lg:text-6xl"
                      cursorClassName="bg-gray-400"
                    />
                  </div>
                  
                  {/* SOUS-TITRE */}
                  <motion.div
                    initial={{ opacity: 0, y: 20 }}
                    animate={{ opacity: 1, y: 0 }}
                    transition={{ duration: 0.8, delay: 3 }}
                    className="space-y-4"
                  >
                    <h2 className="text-2xl lg:text-3xl text-gray-300 font-light">
                      L'IA conversationnelle qui transforme votre{" "}
                      <span className="text-gray-100 font-semibold">churn en croissance</span>
                    </h2>
                    <p className="text-lg text-gray-400 leading-relaxed max-w-2xl">
                      <span className="text-gray-200 font-medium">-67% de churn</span>, 
                      <span className="text-gray-200 font-medium"> +120k€ de revenus annuels</span>, 
                      <span className="text-gray-200 font-medium"> ROI 5x garanti</span>
                    </p>
                  </motion.div>

                  {/* CTA BUTTONS */}
                  <motion.div
                    initial={{ opacity: 0, y: 20 }}
                    animate={{ opacity: 1, y: 0 }}
                    transition={{ duration: 0.8, delay: 4 }}
                    className="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start"
                  >
                    <button 
                      onClick={() => document.getElementById('solution')?.scrollIntoView({ behavior: 'smooth' })}
                      className="px-8 py-4 bg-gray-900 hover:bg-gray-800 text-gray-100 font-medium rounded-lg hover:scale-105 transition-all duration-300 shadow-lg flex items-center justify-center space-x-2"
                    >
                      <VscPlay className="h-5 w-5" />
                      <span>Voir la Démo</span>
                    </button>
                    <button 
                      onClick={() => document.getElementById('roi')?.scrollIntoView({ behavior: 'smooth' })}
                      className="px-8 py-4 border border-gray-600 text-gray-300 font-medium rounded-lg hover:bg-gray-800 hover:text-gray-100 transition-all duration-300 flex items-center justify-center space-x-2"
                    >
                      <VscArrowUp className="h-5 w-5" />
                      <span>Calculer mon ROI</span>
                    </button>
                  </motion.div>
                </div>

                {/* DROITE - AVATAR 3D (Repositionné) */}
                <motion.div
                  initial={{ opacity: 0, scale: 0.8 }}
                  animate={{ opacity: 1, scale: 1 }}
                  transition={{ duration: 1.2, delay: 1.5 }}
                  className="flex justify-center"
                >
                  <div className="relative">
                    <div className="relative w-64 h-64 lg:w-72 lg:h-72">
                      <Avatar3D size={280} />
                    </div>
                    
                    {/* Floating Stats */}
                    {statsData.map((stat, index) => (
                      <motion.div
                        key={index}
                        initial={{ opacity: 0, scale: 0 }}
                        animate={{ opacity: 1, scale: 1 }}
                        transition={{ duration: 0.6, delay: 2 + index * 0.2 }}
                        className="absolute bg-black/80 backdrop-blur-md border border-neutral-700 rounded-lg p-3 text-center"
                        style={{
                          top: `${15 + index * 20}%`,
                          left: index % 2 === 0 ? '-20%' : '120%',
                        }}
                      >
                        <div className="text-2xl">{stat.icon}</div>
                        <div className="text-xl font-bold text-white">{stat.number}</div>
                        <div className="text-xs text-neutral-400">{stat.label}</div>
                      </motion.div>
                    ))}
                  </div>
                </motion.div>
              </div>
            </div>
          </div>
        </BackgroundGradientAnimation>
      </section>

      {/* 📊 SOCIAL PROOF */}
      <section className="py-20 bg-neutral-950">
        <div className="container mx-auto px-4">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold mb-6 bg-gradient-to-r from-blue-400 to-green-400 bg-clip-text text-transparent">
              Résultats Clients Prouvés
            </h2>
            <p className="text-xl text-neutral-300 max-w-3xl mx-auto">
              Nos clients voient des résultats dès le premier mois
            </p>
          </motion.div>

          <div className="grid grid-cols-2 lg:grid-cols-4 gap-8">
            {statsData.map((stat, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.6, delay: index * 0.1 }}
                viewport={{ once: true }}
                className="text-center p-6 bg-gradient-to-br from-neutral-900 to-neutral-800 rounded-xl border border-neutral-700"
              >
                <div className="text-4xl mb-4">{stat.icon}</div>
                <div className="text-3xl font-bold text-white mb-2">{stat.number}</div>
                <div className="text-neutral-400">{stat.label}</div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* 😰 PROBLÈME */}
      <section className="py-20 bg-black">
        <div className="container mx-auto px-4">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold mb-6 text-red-400">
              Le Problème que Vous Vivez
            </h2>
            <p className="text-xl text-neutral-300 max-w-3xl mx-auto">
              67% des membres abandonnent. Vos coûts explosent. Votre croissance stagne.
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            {problemPoints.map((problem, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.6, delay: index * 0.1 }}
                viewport={{ once: true }}
                className="bg-gradient-to-br from-red-900/20 to-orange-900/20 border border-red-500/30 rounded-xl p-6 text-center hover:scale-105 transition-transform duration-300"
              >
                <div className="text-4xl mb-4">{problem.icon}</div>
                <h3 className="text-xl font-bold text-white mb-3">{problem.title}</h3>
                <p className="text-neutral-300 mb-3">{problem.description}</p>
                <div className="text-red-400 font-semibold text-sm">{problem.impact}</div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* 💡 SOLUTION */}
      <section id="solution" className="py-20 bg-neutral-950">
        <div className="container mx-auto px-4">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold mb-6 bg-gradient-to-r from-green-400 to-blue-400 bg-clip-text text-transparent">
              La Solution JARVIS
            </h2>
            <p className="text-xl text-neutral-300 max-w-3xl mx-auto">
              IA conversationnelle qui transforme chaque interaction en opportunité de fidélisation
            </p>
          </motion.div>

          <div className="space-y-16">
            {solutionFeatures.map((feature, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, x: index % 2 === 0 ? -50 : 50 }}
                whileInView={{ opacity: 1, x: 0 }}
                transition={{ duration: 0.8 }}
                viewport={{ once: true }}
                className={`grid grid-cols-1 lg:grid-cols-2 gap-12 items-center ${
                  index % 2 === 1 ? 'lg:flex-row-reverse' : ''
                }`}
              >
                <div className={`space-y-6 ${index % 2 === 1 ? 'lg:order-2' : ''}`}>
                  <div className="flex items-center space-x-4">
                    {feature.icon}
                    <h3 className="text-3xl font-bold text-white">{feature.title}</h3>
                  </div>
                  <p className="text-xl text-neutral-300">{feature.description}</p>
                  <ul className="space-y-3">
                    {feature.benefits.map((benefit, benefitIndex) => (
                      <li key={benefitIndex} className="flex items-center space-x-3">
                        <VscShield className="h-5 w-5 text-green-400" />
                        <span className="text-neutral-200">{benefit}</span>
                      </li>
                    ))}
                  </ul>
                </div>
                
                <div className={`${index % 2 === 1 ? 'lg:order-1' : ''}`}>
                  <div className="bg-gradient-to-br from-neutral-900 to-neutral-800 rounded-2xl p-8 border border-neutral-700">
                    <div className="aspect-video bg-gradient-to-br from-blue-500/20 to-green-500/20 rounded-lg flex items-center justify-center">
                      <VscPlay className="h-16 w-16 text-white/70" />
                    </div>
                  </div>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* 🛠️ DÉMONSTRATION MACBOOK SCROLL */}
      <section id="demo" className="py-20 bg-black">
        <div className="container mx-auto px-4">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-4">
              Découvrez JARVIS en action
            </h2>
            <p className="text-xl text-neutral-400">
              Interface immersive pour vos adhérents
            </p>
          </motion.div>

          <MacbookScroll
            title="Interface JARVIS"
            badge={
              <div className="bg-gray-800/90 text-gray-200 px-4 py-2 rounded-full text-sm font-medium border border-gray-600">
                ⚡ IA Conversationnelle 24/7
              </div>
            }
            src="/images/dashboard-gerant.jpg"
            showGradient={false}
          />
        </div>
      </section>

      {/* 💰 ROI SECTION */}
      <section id="roi" className="py-20 bg-black">
        <div className="container mx-auto px-4">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold mb-6 bg-gradient-to-r from-yellow-400 to-green-400 bg-clip-text text-transparent">
              ROI Garanti 5x
            </h2>
            <p className="text-xl text-neutral-300 max-w-3xl mx-auto">
              Calcul basé sur nos clients actuels - Résultats moyens constatés
            </p>
          </motion.div>

          <div className="space-y-8">
            {roiCalculation.map((calc, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.6, delay: index * 0.1 }}
                viewport={{ once: true }}
                className="bg-gradient-to-r from-neutral-900 to-neutral-800 border border-neutral-700 rounded-xl p-6"
              >
                <div className="grid grid-cols-1 md:grid-cols-5 gap-4 items-center text-center">
                  <div>
                    <h4 className="font-bold text-white">{calc.metric}</h4>
                  </div>
                  <div>
                    <div className="text-red-400 font-semibold">AVANT</div>
                    <div className="text-neutral-300">{calc.before}</div>
                  </div>
                  <div>
                    <div className="text-green-400 font-semibold">AVEC JARVIS</div>
                    <div className="text-neutral-300">{calc.after}</div>
                  </div>
                  <div>
                    <div className="text-blue-400 font-bold">{calc.impact}</div>
                  </div>
                  <div>
                    <div className="text-yellow-400 font-bold text-xl">{calc.value}</div>
                  </div>
                </div>
              </motion.div>
            ))}
          </div>

          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.5 }}
            viewport={{ once: true }}
            className="mt-12 text-center"
          >
            <div className="bg-gradient-to-r from-green-900/50 to-yellow-900/50 border border-green-500 rounded-2xl p-8">
              <h3 className="text-3xl font-bold text-white mb-4">Gain Total Annuel</h3>
              <div className="text-6xl font-bold bg-gradient-to-r from-green-400 to-yellow-400 bg-clip-text text-transparent">
                +175k€
              </div>
              <p className="text-neutral-300 mt-4">Pour un investissement moyen de 35k€</p>
            </div>
          </motion.div>
        </div>
      </section>

      {/* 📞 CTA FINAL */}
      <section id="contact" className="py-20 bg-gradient-to-br from-blue-900/20 to-green-900/20">
        <div className="container mx-auto px-4">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: true }}
            className="text-center"
          >
            <h2 className="text-4xl md:text-6xl font-bold mb-6 text-white">
              Prêt à Multiplier votre ROI ?
            </h2>
            <p className="text-xl text-neutral-300 mb-12 max-w-2xl mx-auto">
              Réservez votre démo personnalisée et découvrez comment JARVIS peut transformer votre salle
            </p>

            <div className="flex flex-col sm:flex-row gap-6 justify-center">
              <a
                href="mailto:contact@jarvis-group.net"
                className="inline-flex items-center justify-center px-12 py-6 bg-gradient-to-r from-green-500 to-blue-500 text-white font-bold text-xl rounded-lg hover:scale-105 transition-transform duration-300 shadow-2xl hover:shadow-green-500/25"
              >
                🚀 Réserver ma Démo Gratuite
              </a>
              <a
                href="tel:+33123456789"
                className="inline-flex items-center justify-center px-12 py-6 border-2 border-blue-400 text-blue-400 font-bold text-xl rounded-lg hover:bg-blue-400 hover:text-white transition-all duration-300"
              >
                📞 Appelez-nous
              </a>
            </div>

            <p className="text-neutral-400 mt-8">
              🔒 Sans engagement • 💰 Audit ROI offert • ⚡ Réponse sous 24h
            </p>
          </motion.div>
        </div>
      </section>
    </div>
  );
}
